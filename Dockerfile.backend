# Dockerfile para el backend
FROM node:18-alpine

WORKDIR /app

# Copiar package.json y package-lock.json
COPY package*.json ./

# Instalar dependencias
RUN npm install

# Copiar el código fuente
COPY . .

# Exponer el puerto (asumiendo que el backend usa 3000 o similar)
EXPOSE 3000

# Comando para ejecutar
CMD ["npm", "start"]